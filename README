# Your Project Name

## Description

https://bitespeed.notion.site/Bitespeed-Backend-Task-Identity-Reconciliation-53392ab01fe149fab989422300423199

## Prerequisites
Make sure you have Docker and Docker Compose installed on your system.

## How to Run
1. Clone this repository and run npm i to install all dependencies.

2. Navigate to the project directory.

3. Build and run the app using Docker Compose:

4. The app should now be running and accessible at http://localhost:3000.

## Endpoints
- `POST /identify`: The endpoint for identifying contacts.

## Commands to run 

docker-compose build
docker-compose up -d

#to check logs (can take some time to run the database and for migrations to be successfull! Kindly restart few times to get it successfully running.)

docker logs bitespeed-app-1
docker logs bitespeed-db-1  
